import type { v2f } from "typegpu/data"

export const Position = [] as v2f[]
export const Mass = [] as number[]

export const Source = {
  pos: [] as v2f[],
  lifetime: [] as number[],
  vel: [] as v2f[],
}
